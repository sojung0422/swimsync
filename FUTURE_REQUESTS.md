# 향후 요구사항 (보류된 기능)

이 문서는 논의는 됐지만 지금 당장 구현하지 않기로 한 기능을 기록합니다. 추후 구글 드라이브 등 별도 요구사항 관리 도구로 옮겨도 됩니다.

## 1. 차량 실제 운행거리 자동 추적 (GPS 기반 월별 기름값 계산)

**요청 내용**: 기사가 차량 연비를 입력하고 "운행" 버튼을 누르면, 그 순간부터 실제 주행 거리를 자동으로 판단해 해당 차량의 월별 기름값을 계산.

**보류 사유**: 이 앱은 브라우저 기반 웹앱이며, 브라우저는 탭이 백그라운드로 가거나 화면이 꺼지면 위치추적(GPS)을 지속적으로 유지하지 못합니다. 즉 "운행 버튼을 누른 순간부터 자동으로 거리를 판단"하는 방식은 이 프로토타입 구조에서 안정적으로 구현하기 어렵습니다.

**대안으로 제시했던 방식** (참고용, 채택 여부는 보류): 기사가 운행 시작/종료 시 주행 거리(km)를 직접 입력하면, 등록된 연비와 유가를 기준으로 시스템이 자동으로 월별 기름값을 계산. GPS 자동 추적은 아니지만 최종 결과물(월별 기름값 자동 산출)은 동일하게 얻을 수 있습니다.

## 2. 아이 학원 도착 확인 (QR/AR 체크인)

**요청 내용**: 성인 앱/학부모 앱 논의 중 언급된 아이디어 — 아이가 학원에 도착해서 특정 코드를 스캔(AR/QR)하면, 그 기록이 학부모에게 "아이가 학원에 도착했다"는 증거로 남도록 하는 기능.

**보류 사유**: 별도 논의 없이 우선순위 로드맵에 포함하기보다, 향후 필요 시 별도로 설계/논의하기로 함.

---

*이 문서는 2026-09-08 기준으로 작성되었습니다. 새로운 보류 항목이 생기면 이 문서에 추가해주세요.*
